name: Expand Debug Diagnostics

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-debug-report:
    name: Patch debug report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: exp
          fetch-depth: 0

      - name: Patch app.js
        run: python3 .github/scripts/expand_debug_diagnostics.py

      - name: Verify changes
        run: |
          git diff --check
          git diff -- app.js

      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js
          git commit -m "chore: expand debug report for reading settings"
          git push origin exp
